import React from 'react';

import styles from './styles.less';
import iconImage from './images/logo.png';

const HeaderIcon = () => <img className={styles.icon} src={iconImage} />;

export default HeaderIcon;
