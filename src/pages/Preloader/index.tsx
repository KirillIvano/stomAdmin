import React from 'react';
import styles from './styles.less';

const PagePreloader = () => (
    <div className={styles.pagePreloader}>
        <div className={styles.preloaderContent}></div>
    </div>
);

export default PagePreloader;
